# AnyToPDF — Frontend (Vercel)

React + Tailwind UI for uploading files to convert to PDF or merge PDFs in order.
Configure your backend URL in `.env`:

```
VITE_API_BASE=https://your-backend.onrender.com
```

Then:
```
npm i
npm run dev
```
